pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            maven { url 'https://jitpack.io' }
            url "https://infocert.jfrog.io/artifactory/list/libs-release/"
            credentials {
                username = "Valerio.DeRosa@sia.eu" // Artifactory user name
                password = "Infocert2021!" // Password or API Key
            }
        }
    }
}
rootProject.name = "assistedSelfieTest"
include ':app'
